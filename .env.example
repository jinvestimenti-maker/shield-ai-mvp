APIFY_TOKEN=

# Admin-only video generation (POST /admin/generate-video)
# FAL_KEY: server-side only, never expose to the client. Set on Render too.
FAL_KEY=
# ADMIN_KEY: password for HTTP Basic Auth on /admin/generate-video. Set on Render too.
ADMIN_KEY=
